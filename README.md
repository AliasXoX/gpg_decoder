# GPG Decoder

This project is a very restrictive GPG decoder in python.
The purpose is to dump information about a gpg encrypted key or message. It will not decrypt anything.

I specify "GPG" and not OpenPGP because I use the RFC 4880 which is obsolete since 2024 but still implemented by GnuPG.
